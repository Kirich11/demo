### record 1
A little late, but eh.. let's create a recipe holder with search and shopping list/recipe/diet/cravings generation.

purpose is to research java and spring boot stuff. do as much async as possible.

1 step. Simple crud/DB setup/Docker/Tests. Done.

2 step. Async centralized buses.
    
    Use generics? (Type errosion bit my ass, should use proxyBuilder with AOP to create @Async handlers and shit).
    
    Added runtime validators that map @Command/Query annotations to generic parameters to ensure it cannot be broken by unchecked type casts.

    Need a test example to assert validation works and cannot bootstrap because of runtime exceptions.

TODO: load tests with JMeter to see diff between threadpool and virtual threads?
      Do AOP bus stuff. Use @Async and be simpler, can it reduce boilerplate?

